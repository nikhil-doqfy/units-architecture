---
scope: Units Finance module Phase 1 -- posting engine, Ledger/reporting boundary, and the units-backend<->Finance integration, as a new Django microservice sharing the existing Units Postgres instance
purpose: build-substrate
altitude: feature
updated: 2026-09-01T15:49
---

- (event) Bootstrapped memlog from existing rendered docs/finance/ARCHITECTURE-SPINE.md (status: final, no prior .memlog.md existed -- the original docs/ folder was accidentally deleted before commit and the spine was reconstructed from the authoring conversation on 2026-09-01). Backfilling AD-1 through AD-16 as already-decided context before amending.
- (decision) AD-1 (existing): Function-based views only, no DRF class-based views/serializers. Binds: all Finance API endpoints. Prevents: two API styles coexisting; a future builder reaching for DRF ViewSets. Rule: every Finance endpoint is a function decorated with @api_view, hand-rolled dict serialization (serialize_journal_entry() etc), matching units-backend's convention. No ModelSerializer/APIView/ViewSet. [ADOPTED]
- (decision) AD-2 (existing): Shared Postgres, no owned database. Binds: all Finance data access. Prevents: a second database, sync/replication layer, eventually-consistent copy of Units data. Rule: units-finance connects to the same Postgres instance/database as units-backend via the same root .env vars; owns its own tables inside that database via its own app+migrations; never a separate database or schema-per-service split. [ADOPTED]
- (decision) AD-3 (existing): DB_HOST must resolve inside the Docker network. Binds: deployment config for units_finance. Rule: root .env's DB_HOST must be postgres (compose service name) not localhost, for both units_backend and units_finance once containerized. Pre-existing config item (currently DB_HOST=localhost) that must be fixed as part of this build.
- (decision) AD-4 (existing): LeaseTransaction post_save signal is the sole trigger mechanism. Binds: FR-4 through FR-8. Rule: a single Django post_save signal on LeaseTransaction (new signals.py in units-backend) is the only event source Finance listens to; no polling.
- (decision) AD-5 (existing): Signal calls Finance over synchronous internal HTTP, never direct writes. Binds: units-backend->Finance integration path, FR-4-FR-9. Rule: post_save handler issues sync HTTP POST to Finance's internal API; all posting/accounting rules live only in units-finance; units-backend never imports Finance models or writes Ledger/JournalEntry tables directly.
- (decision) AD-6 (existing): Retry is synchronous in-request, no async infra. Binds: signal handler's HTTP call to Finance. Rule: on failure, retry synchronously with short backoff (0.5s/1s/2s, 2-3 attempts) in the same request cycle; exhausted retries logged loudly for manual reconciliation; alert channel deferred.
- (decision) AD-7 (existing): Internal API requires a shared-secret header. Binds: every endpoint under Finance's /internal/ namespace. Rule: units-backend sends X-Internal-Token (from FINANCE_INTERNAL_TOKEN env var) on every call; Finance rejects (401/403) missing/wrong values.
- (decision) AD-8 (existing): Celery/Redis are not available infrastructure for Phase 1. Binds: all Finance and units-backend integration design decisions this phase. Rule: Celery is configured in units-backend code but has no deployed Redis/worker/beat; nothing in Finance's Phase 1 design may depend on Celery actually running. [ADOPTED]
- (decision) AD-9 (existing): Ownership share lives on UnitOwner, not a Finance side-table. Binds: FR-9 (multi-owner commission split). Rule: ownership_percent is added as a new field on the existing UnitOwner model (property app, units-backend); Finance reads it read-only, never maintains its own unit-owner-share table.
- (decision) AD-10 (existing): Finance service lives at microservices/units-finance as a Django sibling. Binds: repository layout, docker-compose, deployment. Rule: new sibling Django project at microservices/units-finance/, own manage.py, new units_finance service block in root docker-compose.yml with depends_on: postgres.
- (decision) AD-11 (existing): Naming convention: project finance_service, primary app ledger. Binds: Finance's internal Django project/app structure. Rule: Django project package named finance_service, primary domain app named ledger; compose service name remains units_finance regardless (two independent naming layers).
- (decision) AD-12 (existing): Finance has its own requirements.txt and Dockerfile, independent of units-backend's. Binds: Finance's Python dependency set and build. Rule: microservices/units-finance/ has its own requirements.txt/Dockerfile, own image in docker-compose.yml; never shares units-backend's requirements.txt.
- (decision) AD-13 (existing): units-backend's Django 4.0.0 (EOL) is a known accepted out-of-scope risk; Finance pins independently. Binds: Finance's own Stack choice; explicitly does NOT bind units-backend. Rule: units-backend requirements.txt pins django==4.0.0 (EOL since April 2023); upgrading it is out of scope for this build; Finance's own requirements.txt (AD-12) pins a current Django LTS (5.x) instead. [ADOPTED] Independently corroborated by the platform-wide spine's own Stack/Deferred findings.
- (decision) AD-14 (existing): Idempotency keys on the (from_status, to_status) transition, not just (id, target_status). Binds: posting engine (FR-4-FR-9), JournalEntry's schema. Rule: every JournalEntry stores source_lease_transaction_id and source_status_transition (exact FROM->TO pair); duplicate check is on the exact (lease_transaction_id, from_status, to_status) tuple only.
- (decision) AD-15 (existing): UnitOwner.ownership_percent is NOT NULL with a mandatory backfill, never silently assumed. Binds: UnitOwner migration (units-backend), FR-9's split math (Finance). Rule: field added NOT NULL, no default, same migration backfills every existing multi-owner Unit's UnitOwner rows to sum to 100; posting engine assumes this holds, fails loudly (no partial post) if violated at posting time.
- (decision) AD-16 (existing): JournalEntry reversals are additive-only, never mutate-in-place. Binds: FR-6 (bounce reversal), all balance-reporting queries (FR-10-FR-13). Rule: a reversal posts a new JournalEntry with reversed_journal_entry_id FK to the original; original never edited/deleted; every balance query sums all rows unconditionally, reversals net out through summation.
- (event) Inheriting parent spine: _bmad-output/planning-artifacts/architecture/architecture-units-architecture-2026-09-01/ARCHITECTURE-SPINE.md (Units Platform, initiative altitude, status final). Parent AD-1 through AD-5 are now binding, read-only constraints on this feature-altitude spine. Logging each below by its original parent id -- never re-derived, never renumbered.
- (constraint) Inherited parent AD-1 (Cross-app data access, backend): domain apps MAY import/query other apps' models directly via Django ORM; no repository/service layer required. Binds here: Finance's read-only ORM access to units-backend's Lease/LeaseTransaction/Unit/Charge/Bank/UnitOwner tables (already this spine's own AD-2/AD-5 pattern) is consistent with the parent's convention -- no conflict, ratifies what Finance's spine already does.
- (constraint) Inherited parent AD-2 (Access control enforcement, backend): views MUST scope querysets/access checks inline via utilities.org_scope.get_pmc_ids_for_user(), never a hand-rolled equivalent; DRF permission classes are disabled project-wide. Binds here: Finance's external reporting endpoints (Trial Balance, P&L, Balance Sheet, Ageing) MUST call get_pmc_ids_for_user() to scope results to the requesting user's reachable PMCs/Companies -- this was previously an open Deferred item in Finance's own spine (per-actor reporting visibility) and is now resolved by inheritance, not left open.
- (constraint) Inherited parent AD-3 (API response envelope, backend): all views MUST return via utilities.helper_functions.prepare_response() -> {content, message, status}, never DRF's raw Response; paginated content carries pagination via prepare_response()'s paginator argument, never nested inside content. Binds here: every Finance endpoint (internal sync AND external reporting) must return this exact envelope shape -- conflicts with and overrides Finance's own AD-1's hand-rolled response-shape gap and the Consistency Conventions table's bespoke {error:...} draft.
- (constraint) Inherited parent AD-4 (Role model, backend) and AD-5 (Role-based access, frontend): roles are UserProfile subclasses (Owner/PropertyManager/Tenant), never a role field on auth.User; frontend gating via route guards + PermissionService. Binds here: no conflict -- Finance's spine does not touch role modeling or frontend structure; these parent ADs apply by default with nothing to reconcile.
- (decision) Amend AD-1 in place (id stable, not renumbered): add explicit response-envelope conflict resolution. Finance's AD-1 governed view/serializer style (function-based, no DRF classes) but left response BODY shape unspecified -- the reconstruction note flagged this as the one open conflict. Resolution: AD-1's Rule now explicitly states every Finance endpoint returns via units-backend's utilities.helper_functions.prepare_response() (inherited parent AD-3), never a hand-rolled dict shape. Finance's serialize_*() functions produce the *inner* content payload only; prepare_response() wraps it. Prevents: Finance inventing its own {data:...} or {error:...} shape that the shared frontend interceptor (which expects {content, message, status} project-wide) can't parse.
- (decision) New AD-17: External reporting endpoint auth is the same stateless JWT as units-backend. Binds: Finance's external reporting endpoints (Trial Balance, P&L, Balance Sheet, Ageing) -- explicitly distinct from AD-7's internal-only X-Internal-Token, which continues to govern the units-backend->Finance sync path only. Prevents: Finance inventing a second, parallel auth mechanism for user-facing endpoints that units-frontend's existing http.interceptor.ts (which already attaches Authorization: Bearer <token> project-wide) wouldn't work against unmodified. Rule: Finance's reporting views validate the same JWT units-backend issues (utilities.jwt_token.py, pyjwt, Authorization: Bearer <token>) -- Finance decodes/verifies using the same secret/algorithm as units-backend (shared via the same root .env), not a separate Finance-issued token. [ADOPTED, resolves reconstruction note's auth ambiguity]
- (decision) New AD-18: Reporting endpoints are PMC-scoped via inherited parent AD-2, resolving the prior Deferred 'per-actor reporting visibility' open question. Binds: FR-10 through FR-13 (Trial Balance, P&L, Balance Sheet, Ageing). Prevents: an Owner or Tenant querying Finance's reporting API and seeing another PMC's Companies/JournalEntries; a second, Finance-local reimplementation of PMC-reachability logic diverging from units-backend's. Rule: every reporting view calls utilities.org_scope.get_pmc_ids_for_user() (imported/called the same way units-backend does, per inherited AD-2) and filters Company/JournalEntry results to PMCs the requesting user can reach, before any Company-level scoping Finance's own AD-2/AD-9 already does. This does NOT itself resolve whether an Owner's Trial Balance should redact PMC-internal commission detail (PRD Open Question 9) -- that's a finer-grained redaction question within the now-resolved PMC boundary, and stays open (see Deferred update).
- (event) Update reconciles all conflicts named in the reconstruction note: AD-1 amended (envelope), AD-17 added (JWT auth for reporting endpoints), AD-18 added (PMC-scoping for reporting, resolving prior Deferred item on per-actor visibility down to a narrower open sub-question). Consistency Conventions table's Data&formats row and State/cross-cutting row need corresponding edits to stop describing the old bespoke error shape and unspecified session/token auth. Sources (prd.md) are unaffected by this change -- the update only reconciles the ARCHITECTURE layer against the newly-authored platform spine, not requirements; no upstream PRD edit needed.
- (event) Reviewer gate run on the inheritance update: lint 3 low-severity false positives ({id} in URL path, not template placeholder -- no action). Rubric walker: PASS clean -- inherited AD ids unrenumbered/read-only, AD-1 amendment coherent, AD-17/AD-18 Binds/Prevents/Rule-complete, AD-7 stays internal-only, Deferred item genuinely narrows. Version reviewer: PARTIAL FAIL -- prepare_response()/get_pmc_ids_for_user()/pyjwt usage all verified correct, but AD-17's core premise is false: SECRET_KEY is a hardcoded literal in property_management/settings.py (not read from .env), and root .env has no SECRET_KEY/JWT_SECRET_KEY entry -- Finance cannot validate units-backend JWTs via a shared-.env-secret as AD-17 currently states. Adversarial reviewer: 6 divergence scenarios, standout HIGH severity is #2 -- Finance's own Company model has no field/FK connecting it to a PMC at all (ERD only shows Unit<->Company 1:1), so AD-18's get_pmc_ids_for_user() PMC-id list has no stated join path to Company rows; two builders would invent different mappings. Also flagged: JWT payload ({user_id,email,exp} per AD-17) has no stated bridge to the UserProfile/role-subclass ORM row get_pmc_ids_for_user() needs; AD-3's pagination rule is conditional and never settles which of the 4 reports (Trial Balance/P&L/Balance Sheet=snapshot documents, Ageing=naturally paginated list) actually paginate; AD-1's error path leaves content-vs-message placement of error detail unpinned; AD-15's backfill has no stated sequencing guarantee vs Finance's independent first deploy; AD-16+AD-18 composition (sum-all-unconditionally + PMC-filter) is never shown as one queryset, riding the same unresolved join-path gap as #2.
- (decision) Amend AD-17 in place (id stable): fix false shared-secret premise. Original rule assumed units-backend's JWT secret is readable from the shared root .env -- verified false (SECRET_KEY is a hardcoded literal in property_management/settings.py, not env-sourced; .env has no SECRET_KEY/JWT_SECRET_KEY entry). Fix: units-backend's SECRET_KEY must be extracted into the root .env as part of this build (a small, explicit change to units-backend, not just Finance), and both services read JWT_SECRET_KEY from .env. This is now an explicit prerequisite step, not an assumed-already-true fact. Prevents: Finance being built against a secret-sharing mechanism that doesn't exist, discovered only at integration time.
- (decision) New AD-19: Company is mapped to PMC via a direct company.pmc_id field, and get_pmc_ids_for_user()'s bare user_id is bridged to a UserProfile via a single lookup helper. Binds: FR-2 (Unit->Company mapping), AD-18's PMC-scoping. Prevents: two reporting endpoints inventing different PMC->Company join paths (direct FK vs multi-hop Unit traversal); a hand-rolled JWT-payload-to-UserProfile bridge diverging from a single blessed helper. Rule: Finance's Company model gains a required pmc_id field (integer, referencing units-backend's PropertyManagmentCompany.id by value, not a cross-database FK) set at Company-creation time (FR-1, ledger/management/commands/); AD-18's scoping filters Company/JournalEntry directly on company.pmc_id in get_pmc_ids_for_user()'s returned list -- never via Unit traversal. Bridging AD-17's JWT payload ({user_id, email, exp}) to the UserProfile row get_pmc_ids_for_user() needs is done by one shared helper (get_user_profile_from_jwt(payload) or equivalent, defined once in Finance's ledger/ and reused by every reporting view) -- never re-derived per-endpoint.
- (decision) Amend AD-1 in place again: pin error-path shape. Adversarial review found prepare_response()'s signature enforces no structure on failure, risking content-vs-message divergence for error detail across Finance and units-backend. Fix: on failure, Finance MUST put a short human-readable summary in message and put structured error detail (e.g. {field: [...]} validation errors) in content, status carries the HTTP status code -- matching units-backend's own existing error-handling precedent, not inventing a new shape. Applies uniformly to both the internal sync endpoint (consumed by units-backend's AD-6 retry/logging) and reporting endpoints.
- (decision) Amend inherited-AD-3 application (Finance-local clarification, not amending the parent): settle which of the 4 reporting endpoints paginate. Trial Balance, P&L, and Balance Sheet are point-in-time/period snapshot documents -- returned whole, never paginated (prepare_response()'s paginator argument omitted). Ageing is a naturally growing list (one row per open item) -- MUST paginate via prepare_response()'s paginator argument per the inherited platform AD-3. Recorded to prevent one report silently truncating a financial statement to a page.
- (question) Two adversarial findings deferred rather than fixed now: (1) AD-7/AD-17 hybrid case -- a foreseeable future dry-run/preview or server-to-server reporting call that doesn't cleanly fit either internal-shared-secret or user-facing-JWT auth. No such endpoint exists in Phase 1 scope; revisit if/when the Deferred 'reporting migration' item is picked up. (2) AD-15's UnitOwner backfill sequencing vs Finance's independent first deploy has no stated ordering guarantee -- same hazard class as the existing DB_HOST cutover Deferred item; revisit together with that item's operational runbook, not architected here.
- (event) Applied all clear reviewer-gate fixes: AD-17 amended to state the SECRET_KEY-sharing prerequisite explicitly (verified not-yet-true) instead of assuming it; new AD-19 added (Company.pmc_id direct mapping + single JWT-to-UserProfile bridge helper), closing the HIGH-severity join-path gap; AD-1 amended again to pin error-path shape (message=summary, content=structured detail, status=HTTP code); Structural Seed gained an explicit per-report pagination rule (3 snapshot docs unpaginated, Ageing paginated); ERD's Company entity now shows pmc_id; Capability map and Consistency Conventions updated to cite AD-19. Two findings deliberately left as new Deferred entries rather than fixed now (AD-7/AD-17 hybrid auth case; AD-15 backfill-vs-Finance-deploy sequencing) -- genuine open questions, not architected away. Re-ran lint: same 3 pre-existing low-severity false positives ({id} URL path params, not template placeholders), no new findings.
- (event) spine finalized
- (decision) Model correction (post-finalize, during epics-and-stories breakdown): the standalone 'Company' model was WRONG for this domain. Corrected: PropertyManagmentCompany ('PMC', existing units-backend model) IS the accounting entity, 1:1 -- no separate Company model. New Finance-owned table FinancePMCProfile holds only accounting settings (base_currency, country, fiscal_year_start_month), keyed by a required pmc_id field holding the value of PropertyManagmentCompany.id (not a cross-DB FK). Chain confirmed: Organization -> PropertyManagmentCompany -> Property -> PropertyBlocks -> Unit (all existing, unmodified). One Organization can have multiple PMCs, each independent books.
- (decision) Old FR-2 (Unit-to-Company mapping table) retired, not renumbered -- marked [REMOVED] in prd.md with a resolution pointer. Unit already resolves to its PMC via the existing Unit.parent_property.pmc chain; no new field/table needed. AD-19 (previously 'Company maps to PMC via a direct pmc_id field') rewritten in place, same id: premise flips from 'resolve a join between Company and PMC' to 'FinancePMCProfile.pmc_id IS the primary key relationship, not a join to resolve.' The JWT-payload-to-UserProfile bridge-helper half of AD-19 is unchanged, still required.
- (decision) New Deferred item: Organization-level consolidated reporting (summing across all PMCs under one Organization via PropertyManagmentCompany.organization_id) is explicitly Phase 2, not Phase 1. Phase 1 reporting (FR-10-FR-13) stays scoped strictly per-FinancePMCProfile/per-PMC. Achievable later as an aggregation query, not a data model change, since FinancePMCProfile.pmc_id already anchors correctly.
- (decision) New Deferred item: units-frontend UI work is explicitly Phase 2, not Phase 1. Phase 1 ships Finance backend + reporting API only, verified via Swagger/Postman/automated tests -- no Angular screens built. PRD's UJ-1 (Owner/PMC checking Trial Balance in the UI) remains a real intended future journey in the document, just moved to Phase 2 scope, not removed.
- (event) Applied model correction to both docs/finance/prd.md and docs/finance/ARCHITECTURE-SPINE.md (via a forked agent, full diff summarized in docs/finance/.model-correction-note.md): PRD Glossary/FR-1/FR-2(retired)/FR-3-FR-15 wording, scope sections, Assumptions Index; Spine's Design Paradigm, mermaid flowchart, AD-2/AD-5/AD-11/AD-18/AD-19 (AD-19 rewritten in place), ERD (Company entity renamed FinancePMCProfile, dropped duplicate name field, removed Unit||--||Company edge), source tree, Capability map, Deferred section. Re-ran lint: clean, same 3 pre-existing low-severity {id} URL-path false positives, no new findings. Both files' frontmatter updated dates and top-of-file revision notes added for traceability. This correction was caught during the bmad-create-epics-and-stories workflow (step 1, requirement extraction) when the user reviewed the extracted requirements and flagged the Company/PMC domain error before epic design began -- caught before any story was written against the wrong model.
- (decision) Amend AD-17 in place again (id stable): decouple JWT signing from Django's general-purpose SECRET_KEY. Caught by user during epics/stories Story 3.1 review: utilities/config.py currently sets JWT_SECRET_KEY = settings.SECRET_KEY, so sharing SECRET_KEY with Finance (as AD-17 previously specified) would hand Finance the ability to forge units-backend sessions/CSRF tokens too, not just verify JWTs -- broader blast radius than intended. Fix: units-backend gets a NEW dedicated JWT_SECRET_KEY env var (fresh random value, independent of SECRET_KEY), config.py changed to read it via os.getenv('JWT_SECRET_KEY') instead of aliasing settings.SECRET_KEY, only this narrower secret goes into the shared root .env. Django's SECRET_KEY itself is never extracted into .env and never shared with Finance. Updated the Deferred section's .env cutover notes (DB_HOST + JWT_SECRET_KEY, was DB_HOST + SECRET_KEY) to match.
- (event) Step 3 (story generation) complete: all 4 epics, 17 stories generated and approved. Epic 1 (Foundation/PMC Activation): 3 stories, FR-1/FR-3. Epic 2 (Posting Engine): 7 stories, FR-4-FR-9 + units-backend integration; Story 2.1 documents that the UI-facing cheque status-change API (lease_cheque_view, aliased by legacy lease_cheque_status) already exists in units-backend -- no new status-change API needed, only the new post_save signal reacting to it. Epic 3 (Reporting API): 5 stories, FR-10-FR-13; Story 3.1 caught and fixed a real security gap during review -- AD-17 originally planned to share Django's general-purpose SECRET_KEY with Finance, but JWT_SECRET_KEY was found to just alias settings.SECRET_KEY in config.py, so sharing it would let Finance forge units-backend sessions/CSRF tokens too. Fixed: dedicated JWT_SECRET_KEY env var decoupled from SECRET_KEY, AD-17 amended in place, Deferred section's cutover notes corrected. Epic 4 (Bank Reconciliation): 2 stories, FR-14/FR-15; Story 4.2 revised to add manual match/un-reconcile capability beyond the heuristic's suggestions, per user request.
- (event) bmad-create-epics-and-stories workflow complete. Step 4 final validation passed: FR coverage complete (all 14 FRs mapped, FR-2 correctly retired), no starter template applies, tables created only when needed per story, no forward dependencies within or across epics, Epic 2's repeated posting.py/models.py touches are correct single-component ordered stories not incidental overlap. docs/finance/epics.md finalized: 4 epics, 17 stories.
